export default function IndexPage() {
  return (
    <div className="my-8">
      <p>
        Playing audio on the web is easy enough... however when playing audio after an async
        operation might not work as you would expect!
      </p>
      <p className="mt-4">
        Use the links above to see what happens and read the post about how to work around the issue
      </p>
    </div>
  );
}
